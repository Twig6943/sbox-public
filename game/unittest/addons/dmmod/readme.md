
This addon references online package "facepunch.dm98"

To prove:

1. Local uncompiled projects can reference remote packages
2. Local projects can compile using remote packages
3. Local projects can use remote package's base.dll instead of the local